def foo():
    print("General Kenobi!")


class R2D2Step:
    step = "Example step"
    # this is what is shown in the CLI options

    def __init__(self, repo, repo1, console, config):
        self.repo = repo
        self.repo1 = repo1
        self.console = console
        self.config = config

    def bar(self):
        print("Why hello there!")

    # def prerun(self):
    #     # Optional.
    #     # Things to be done before running all steps. all_test.py uses this to select all other tests, for example.
    #     pass.

    def run(self):
        # this is the "main" method, and will be called by the plugin system
        self.bar()
        foo()
